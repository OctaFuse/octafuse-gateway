# 本地 `npm run preview` / `npm run dev:admin` 时 Wrangler 会合并此文件（勿提交真实生产密码）。
# 若本文件不存在，`scripts/ensure-dev-vars.mjs` 会自动生成同名 `.dev.vars`（默认 admin / admin）。
# Cloudflare 运行时仅支持 D1（由 wrangler 绑定 DB），不要在这里配置 DATABASE_URL。
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
