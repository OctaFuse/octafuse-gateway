# 真源：仓库根 `.env.example`。此处为仅在 `packages/proxy` 内执行 `npm run dev:node` 时的最小示例。
#
# 推荐在仓库根维护 `.env`，并执行 `npm run dev:proxy:node`。
# 若仅在本包执行 `npm run dev:node`，可复制变量到本文件并取消注释，或：`ln -s ../../.env .env`
#
# DATABASE_DRIVER=postgres
# DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/octafuse
# PORT=8787
#
# 可选：文本流式超时（毫秒）。空则代码默认 2min / 30s / 10min。
# Docker 若要更宽：STREAM_FIRST_CHUNK_TIMEOUT_MS=180000 STREAM_IDLE_TIMEOUT_MS=45000 USAGE_SAFETY_TIMEOUT_MS=900000
#
# Web Search（POST /v1/tools/web-search）在 Admin → Tools → Configuration
# 写入 system_config，无需环境变量。
