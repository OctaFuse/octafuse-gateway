diff --git a/node_modules/@opennextjs/cloudflare/dist/cli/build/patches/plugins/next-server.js b/node_modules/@opennextjs/cloudflare/dist/cli/build/patches/plugins/next-server.js
index d816e83..b2c04e6 100644
--- a/node_modules/@opennextjs/cloudflare/dist/cli/build/patches/plugins/next-server.js
+++ b/node_modules/@opennextjs/cloudflare/dist/cli/build/patches/plugins/next-server.js
@@ -28,6 +28,10 @@ export function patchNextServer(updater, buildOpts) {
                 contents = patchCode(contents, createComposableCacheHandlersRule(composableCacheHandler));
                 // Node middleware are not supported on Cloudflare yet
                 contents = patchCode(contents, disableNodeMiddlewareRule);
+                // `dir: __dirname` is bundled as `""`; `path.resolve("")` becomes `/` in workerd so Next
+                // tries `require("/.next/server/middleware-manifest.json")` on RSC requests (500).
+                // This app has no `middleware.ts`; return the same JSON as an empty middleware manifest.
+                contents = patchCode(contents, inlineEmptyMiddlewareManifestRule);
                 contents = patchCode(contents, attachRequestMetaRule);
                 return contents;
             },
@@ -45,6 +49,17 @@ fix: |-
     // patched by open next
   }
 `;
+export const inlineEmptyMiddlewareManifestRule = `
+rule:
+  pattern:
+    selector: method_definition
+    context: "class { getMiddlewareManifest($$$PARAMS) { $$$_ } }"
+fix: |-
+  getMiddlewareManifest($$$PARAMS) {
+    if (this.minimalMode) return null;
+    return {"version":3,"middleware":{},"sortedMiddleware":[],"functions":{}};
+  }
+`;
 export const buildIdRule = `
 rule:
   pattern:
diff --git a/node_modules/@opennextjs/cloudflare/dist/cli/build/bundle-server.js b/node_modules/@opennextjs/cloudflare/dist/cli/build/bundle-server.js
index 8cb687d..17f26aa 100644
--- a/node_modules/@opennextjs/cloudflare/dist/cli/build/bundle-server.js
+++ b/node_modules/@opennextjs/cloudflare/dist/cli/build/bundle-server.js
@@ -102,10 +102,12 @@ export async function bundleServer(buildOpts, projectOpts) {
         ],
         external: [
             "./middleware/handler.mjs",
-            // Do not bundle og when it is not used
-            ...(useOg ? [] : ["next/dist/compiled/@vercel/og/index.edge.js"]),
         ],
         alias: {
+            // Do not let Wrangler resolve and bundle @vercel/og when it is not used.
+            ...(useOg ? {} : {
+                "next/dist/compiled/@vercel/og/index.edge.js": path.join(buildOpts.outputDir, "cloudflare-templates/shims/throw.js"),
+            }),
             // Workers have `fetch` so the `node-fetch` polyfill is not needed
             "next/dist/compiled/node-fetch": path.join(buildOpts.outputDir, "cloudflare-templates/shims/fetch.js"),
             // Workers have builtin Web Sockets
