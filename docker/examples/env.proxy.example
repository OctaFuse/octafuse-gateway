# Copy to .env.gateway for gateway.proxy.yml
#
# CI 推送命名见 .github/workflows/octafuse-docker-images.yml（本组织默认如下）。
GATEWAY_PROXY_IMAGE=ghcr.io/octafuse/octafuse-gateway-proxy:latest
# 任意私有 OCI registry（如自建 Harbor）：按需 mirror / retag，例如：
# GATEWAY_PROXY_IMAGE=registry.example.com/example-org/octafuse-gateway-proxy:latest

# `--profile migrate` 时需要：
GATEWAY_MIGRATE_IMAGE=ghcr.io/octafuse/octafuse-gateway-migrate:latest
# GATEWAY_MIGRATE_IMAGE=registry.example.com/example-org/octafuse-gateway-migrate:latest

# 宿主机映射端口（默认 18787）；容器内固定 8787
GATEWAY_PROXY_PORT=18787

# 外置 Postgres（云 RDS / 自建实例等）；MySQL 时改为 mysql:// 并设 DATABASE_DRIVER=mysql
DATABASE_URL=postgres://gateway_user:change-me@db.example.com:5432/octafuse
DATABASE_DRIVER=postgres

# 可选：容器启动前自动迁移（Zeabur / 自托管推荐）。默认关闭。
# AUTO_MIGRATE=1

# 迁移（备选）：docker compose --profile migrate run --rm migrate
